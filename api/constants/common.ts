const TIMEZONE = {
  INDIA: 'Asia/Calcutta'
}

const ROLE = {
  ADMIN: 'admin',
  USER: 'user'
}

const STATUS = {
  ACTIVE: 'active',
  INACTIVE: 'inactive',
  DELETED: 'deleted',
  INVALID: 'invalid',
  UPDATED: 'updated',
  PENDING: 'pending',
  REPLIED: 'replied'
}

export default { TIMEZONE, ROLE, STATUS }
